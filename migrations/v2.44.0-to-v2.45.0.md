# Migration v2.44.0 → v2.45.0

> Data: 2026-04-15
> Tipo: minor

## Resumo

Git isolation por task no task-runner (MO4): branch isolada opt-in para cada task.

## Pre-requisitos

- Versao atual do projeto: v2.44.0
- Se estiver em versao anterior, aplique as migrations anteriores primeiro

## Mudancas

### 🔄 Overwrite (substituir arquivo inteiro)

#### 1. `agents/task-runner.md`

**O que mudou:** nova secao "Git isolation (opt-in)" com instrucoes para criar branch `task/{spec-id}-t{index}`, regras de merge humano, e campo "Git isolation" no output template.

**Como aplicar:** copiar do framework.

---

### 📝 Structural — secoes novas

#### 2. `skills/context-fresh/README.md`

**O que mudou:** campo `git_isolation` adicionado ao template de briefing (secao 2). Nota sobre perguntar ao dev no inicio da sessao.

**Como aplicar:** o `/update-framework` detecta e aplica secoes novas automaticamente. Se nao funcionar, adicionar manualmente:
- Campo "Git isolation" no template de briefing (apos "Skills a seguir")
- Nota sobre perguntar ao dev (antes de "3. Despachar")

---

## Framework-tags

```bash
grep -rl "framework-tag: v2.44.0" --include="*.md" . | grep -v "migrations/" | xargs sed -i 's/framework-tag: v2.44.0/framework-tag: v2.45.0/g'
```

## Verificacao

- [ ] task-runner tem secao "Git isolation (opt-in)"
- [ ] context-fresh tem campo git_isolation no briefing
- [ ] Framework-tags atualizados para v2.45.0
