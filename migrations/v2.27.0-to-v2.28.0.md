# Migration: v2.27.0 → v2.28.0

Esta migration cobre as mudanças que chegam a projetos downstream via `/update-framework`.

---

## Novos arquivos

### `.claude/skills/map-codebase/SKILL.md`

**Estratégia:** structural (novo arquivo — entregue automaticamente pelo setup/update)

Nova skill `/map-codebase` para análise paralela de codebase em 4 dimensões: stack tecnológico, arquitetura, convenções de código e concerns ativos. Útil para onboarding em projetos existentes ou após longa ausência.

Ao rodar `/update-framework`, o arquivo será criado em `.claude/skills/map-codebase/SKILL.md` no projeto.

**Ação necessária:** nenhuma. O update aplica automaticamente.

---

## Content patches (mudanças dentro de seções existentes)

> O merge structural adiciona/remove seções H2/H3 — mas não atualiza conteúdo dentro de seções existentes. Estas mudanças precisam ser aplicadas manualmente.

### CLAUDE.md — skill `/map-codebase` na tabela de skills

**Arquivo:** `CLAUDE.md` do projeto (estratégia `manual`)

**Mudança:** adição da linha 25 na tabela de skills:

```markdown
| 25 | Vai iniciar trabalho em projeto desconhecido ou após longa ausência? | `/map-codebase` — mapeamento de stack, arquitetura e convencoes | Recomendado |
{25+. Skills específicas do domínio do projeto}
```

Substituir o placeholder `{24+. Skills específicas do domínio do projeto}` pela linha acima seguida do novo placeholder `{25+. ...}`.

**Como aplicar:** o `/update-framework` mostrará o diff para aplicação manual (CLAUDE.md é estratégia `manual`).
