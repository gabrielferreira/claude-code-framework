# Migration v2.49.2 → v2.49.3

> Data: 2026-04-17
> Tipo: patch

## Resumo

Patch com correções de URLs do repositório (após transferência de namespace) e ortografia em arquivos distribuídos. Nada exige migração manual — `/update-framework` aplica automaticamente via strategies do MANIFEST.

## Pre-requisitos

- Versão atual do projeto: v2.49.2
- Se estiver em versão anterior, aplique as migrations anteriores primeiro

## Mudanças

### 🔄 Overwrite (substituir arquivo inteiro)

#### 1. `.claude-plugin/plugin.json` e `.claude-plugin/marketplace.json`

**O que mudou:** URLs `repository`/`homepage`/`source.repo` atualizadas para `github.com/gabrielferreira/claude-code-framework`. Campos `author`/`owner.name` atualizados para "Gabriel Ferreira".

**Como aplicar:** `/update-framework` aplica automaticamente (strategy `overwrite`).

**Impacto se não aplicar:** se o projeto usa plugin.json para publicar plugin próprio, as URLs ficarão apontando pro namespace antigo (inacessível).

---

### 📝 Structural — conteúdo atualizado

#### 2. `docs/MIGRATION_GUIDE.md` e `specs/DESIGN_TEMPLATE.md`

**O que mudou:** correções ortográficas — "Estratégia" com acento em headers de tabela e seções.

**Como aplicar:** `/update-framework` aplica automaticamente (strategy `structural`).

**Impacto se não aplicar:** nenhum funcional — apenas ortografia.

---

#### 3. `docs/SETUP_GUIDE.md`

**O que mudou:** URL de `git clone` atualizada para o novo namespace.

**Como aplicar:** `/update-framework` aplica automaticamente.

**Impacto se não aplicar:** nenhum no funcionamento do framework. Usuários novos que tentarem o comando de clone do guide terão erro (repo antigo inacessível).

---

### 👀 Manual (decisão humana)

Nenhum arquivo `manual` mudou nesta versão.

---

### 🆕 Arquivos novos

Nenhum arquivo novo distribuído pra projetos.

Arquivos novos **internos do framework** (não distribuídos): `CODE_OF_CONDUCT.md`, `SECURITY.md`, `.github/ISSUE_TEMPLATE/*`. Relevantes apenas para quem contribui no repo do framework.

---

### 🗑️ Arquivos removidos

Nenhum.

---

## Framework-tags

`/update-framework` atualiza automaticamente. Manualmente:

```bash
grep -rl "framework-tag: v2.49.2" --include="*.md" . | xargs sed -i 's/framework-tag: v2.49.2/framework-tag: v2.49.3/g'
```

## Verificação

- [ ] URLs em `.claude-plugin/plugin.json` do projeto apontam para o novo namespace (se aplicável)
- [ ] Framework-tags alinhados com v2.49.3
- [ ] `scripts/verify.sh` continua passando

---

## Diff de referência (git diff v2.49.2..HEAD)

> Toda a aplicação é automática pelo `/update-framework`.

```
A	.github/ISSUE_TEMPLATE/bug_report.md
A	.github/ISSUE_TEMPLATE/config.yml
A	.github/ISSUE_TEMPLATE/feature_request.md
A	CODE_OF_CONDUCT.md
A	SECURITY.md
M	.claude-plugin/plugin.json
M	.claude-plugin/marketplace.json
M	CLAUDE.md
M	CHANGELOG.md
M	VERSION
M	docs/MIGRATION_GUIDE.md
M	specs/DESIGN_TEMPLATE.md
M	scripts/release.sh
M	<169 arquivos com framework-tag bumpado v2.49.2 → v2.49.3>
```
