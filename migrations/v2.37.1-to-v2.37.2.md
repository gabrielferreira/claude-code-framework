# Migration v2.37.1 → v2.37.2

> Data: 2026-04-10
> Tipo: patch

## Resumo

Adicionada secao `## Checklist` na skill `/quick` — exigida pelo `validate-structure.sh`. Sem ela o CI falhava.

## Pre-requisitos

- Versao atual do projeto: v2.37.1
- Se estiver em versao anterior, aplique as migrations anteriores primeiro

## Mudancas

### 🔧 Content patches — mudancas dentro de secoes existentes

#### 1. `.claude/skills/quick/SKILL.md` — nova secao `## Checklist`

**Motivo:** `validate-structure.sh` exige secao `## Checklist` em todas as skills. A `/quick` nao tinha.

Adicionar apos a secao `## Passos` (apos o item 6 "PR") e antes de `## Regras`:

```markdown
## Checklist

- [ ] Mudança se encaixa nos critérios de quick task (não toca lógica de negócio, auth, schema, ≤3 arquivos)
- [ ] Implementação concluída
- [ ] Testes relevantes passando (se quebrou → não é quick task, criar spec)
- [ ] `verify.sh` executado sem erros
- [ ] Commit com Conventional Commits
- [ ] PR aberto para main (nunca push direto)
```

**Impacto se nao aplicar:** CI `validate-structure.sh` falha na validacao da skill `/quick`.

---

## Framework-tags

Apos aplicar as mudancas, atualize os framework-tags nos arquivos tocados:

```bash
grep -rl "framework-tag: v2.39.0" --include="*.md" . | xargs sed -i 's/framework-tag: v2.39.0/framework-tag: v2.39.0/g'
```

## Verificacao

Apos aplicar, verifique:

- [ ] Skill `/quick` tem secao `## Checklist`
- [ ] Framework-tags atualizados para v2.37.2
- [ ] `scripts/verify.sh` passa sem erros (se aplicavel)
