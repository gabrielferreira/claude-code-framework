# Migration v2.40.0 → v2.41.0

> Data: 2026-04-15
> Tipo: patch

## Resumo

Melhoria linguística no CLAUDE.template.md inspirada nos Karpathy Guidelines (42k stars). Nova regra comportamental, princípios de simplicidade e mudanças cirúrgicas, execução orientada por critérios verificáveis.

## Pre-requisitos

- Versao atual do projeto: v2.40.0
- Se estiver em versao anterior, aplique as migrations anteriores primeiro

## Mudancas

### 🔧 Content patches — mudancas dentro de secoes existentes

#### 1. `CLAUDE.md` — secao `## Regras de operacao (obrigatorio)`

**Motivo:** nova regra comportamental "Nao assumir. Nao esconder confusao." baseada no princípio Think Before Coding (Karpathy). Numeracao das regras seguintes muda (antiga 4 vira 5, antiga 5 vira 6).

**Texto antigo** (encontrar e substituir):
```markdown
4. **verify.sh antes de commit.** Sem excecoes. Se falhar, corrigir antes de commitar.
5. **STATE.md e memoria entre sessoes.**
```

**Texto novo:**
```markdown
4. **Nao assumir. Nao esconder confusao.** Se o pedido tem multiplas interpretacoes, apresentar — nao escolher silenciosamente. Se algo nao esta claro, parar e perguntar. Se existe abordagem mais simples, dizer. Pushback e esperado quando justificado.
5. **verify.sh antes de commit.** Sem excecoes. Se falhar, corrigir antes de commitar.
6. **STATE.md e memoria entre sessoes.**
```

**Impacto se nao aplicar:** Claude continua assumindo silenciosamente em vez de surfacear ambiguidades.

---

#### 2. `CLAUDE.md` — secao `## Regras de código`

**Motivo:** adição dos princípios "Simplicidade primeiro" e "Mudanças cirúrgicas" com testes de sanidade concretos.

**Texto antigo:**
```markdown
## Regras de código

{Adaptar: regras especificas da stack e do projeto. Exemplos:}

1. **Testes passando = pré-requisito.**
```

**Texto novo:**
```markdown
## Regras de código

**Simplicidade primeiro.** Mínimo de código que resolve o problema. Nada especulativo.

- Sem features alem do que foi pedido.
- Sem abstracoes para codigo de uso unico. Tres linhas similares sao melhores que uma abstracao prematura.
- Sem "flexibilidade" ou "configurabilidade" que ninguem pediu.
- Sem error handling para cenarios impossiveis.
- Se escreveu 200 linhas e podia ser 50, reescrever.

**O teste:** "Um engenheiro senior diria que isto esta complicado demais?" Se sim, simplificar.

**Mudancas cirurgicas.** Tocar so no que precisa. Limpar so a propria sujeira.

- Nao "melhorar" codigo adjacente, comentarios ou formatacao.
- Nao refatorar coisas que nao estao quebradas.
- Seguir o estilo existente, mesmo que faria diferente.
- Se notar dead code nao relacionado, mencionar — nao deletar.
- Cada linha alterada deve rastrear diretamente ao pedido do usuario.

{Adaptar: regras especificas da stack e do projeto. Exemplos:}

1. **Testes passando = pré-requisito.**
```

**Impacto se nao aplicar:** regras de simplicidade e mudanças cirúrgicas não ficam explícitas no CLAUDE.md.

---

## Framework-tags

```bash
grep -rl "framework-tag: v2.40.0" --include="*.md" . | xargs sed -i 's/framework-tag: v2.40.0/framework-tag: v2.41.0/g'
```

## Verificacao

- [ ] Content patches aplicados no CLAUDE.md (regras de operação + regras de código)
- [ ] Framework-tags atualizados para v2.41.0
