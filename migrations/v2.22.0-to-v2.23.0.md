# Migration v2.22.0 → v2.23.0

> Data: 2026-04-09
> Tipo: minor

## Resumo

No modo Notion do `/spec`, os campos Severidade e Estimativa agora são obrigatórios e vêm com sugestão automática baseada na complexidade da spec (Pequeno/Médio/Grande/Complexo). O usuário confirma ou ajusta — não pode deixar em branco.

## Pre-requisitos

- Versao atual do projeto: v2.22.0
- Se estiver em versao anterior, aplique as migrations anteriores primeiro

## Mudancas

### 📝 Structural — content patches

#### 1. `.claude/skills/spec-creator/SKILL.md` — seção `Modo Notion (specs externas via MCP)`, passo 3

**Motivo:** Severidade e Estimativa estavam opcionais (Estimativa aceitava resposta vazia). Agora são obrigatórios e o skill sugere um valor baseado na complexidade antes de perguntar.

**Texto antigo** (passo 3 do modo Notion):
```markdown
3. **Coletar informações para properties** (perguntar ao usuário):
   - Título da spec
   - Domínio, Tipo, Severidade, Fase, Camadas, Impacto
   - Estimativa — sempre perguntar; aceitar resposta vazia para deixar em branco
   - Projeto (nome do repositório atual)
   - **Campos adicionais** — para cada campo na tabela "Campos adicionais" do CLAUDE.md (se existir):
     - `Perguntar ao usuario`: perguntar o valor ao usuário. Se o campo for select, apresentar as opções listadas na coluna "Opcoes" da tabela. Campo obrigatório: bloquear criação até ser preenchido.
```

**Texto novo:**
```markdown
3. **Coletar informações para properties** (perguntar ao usuário):
   - Título da spec
   - Domínio, Tipo, Fase, Camadas, Impacto
   - **Severidade** — obrigatório; sugerir baseado na complexidade e aguardar confirmação (não pode ficar em branco):
     - Pequeno → `baixa`
     - Médio → `media`
     - Grande → `alta`
     - Complexo → `critica`
     - Se `--from` tem prioridade, mapear: Critical→`critica`, High→`alta`, Medium→`media`, Low→`baixa`
   - **Estimativa** — obrigatório; sugerir baseado na complexidade e aguardar confirmação (não pode ficar em branco):
     - Pequeno → `< 4h`
     - Médio → `1-2 dias`
     - Grande → `1-2 semanas`
     - Complexo → `> 2 semanas`
     - Se `--from` tem story points, converter em estimativa legível (ex: 5 SP → `~3 dias`)
   - Projeto (nome do repositório atual)
   - **Campos adicionais** — para cada campo na tabela "Campos adicionais" do CLAUDE.md (se existir):
     - `Perguntar ao usuario`: perguntar o valor ao usuário. Se o campo for select, apresentar as opções listadas na coluna "Opcoes" da tabela. Se o nome do campo indicar severidade ou estimativa/esforço, aplicar a mesma lógica de sugestão por complexidade acima. Campo obrigatório: bloquear criação até ser preenchido.
```

**Impacto se nao aplicar:** Severidade e Estimativa continuam opcionais; usuário pode deixar em branco e não recebe sugestão automática.

---

## Framework-tags

Apos aplicar as mudancas, atualize os framework-tags nos arquivos tocados:

```bash
grep -rl "framework-tag: v2.36.0" --include="*.md" . | xargs sed -i '' 's/framework-tag: v2\.22\.0/framework-tag: v2.36.0/g'
```

## Verificacao

Apos aplicar, verifique:

- [ ] `.claude/skills/spec-creator/SKILL.md` tem o novo texto do passo 3 (Severidade e Estimativa com sugestão por complexidade)
- [ ] Framework-tags atualizados para v2.23.0
