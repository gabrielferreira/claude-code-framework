# Migration v2.48.1 → v2.49.0

> Data: 2026-04-16
> Tipo: minor

## Resumo

Refatoração interna do setup e update SKILL.md — conteúdo condicional (monorepo, Notion, auditoria, exemplos) extraído para arquivos auxiliares. Zero impacto em projetos downstream.

## Pre-requisitos

- Versao atual do projeto: v2.48.1
- Se estiver em versao anterior, aplique as migrations anteriores primeiro

## Mudancas

### 🔄 Overwrite (substituir arquivo inteiro)

#### 1. `skills/update-framework/SKILL.md`

**O que mudou:** Refatorado de 1.257 para 564 linhas. Conteúdo de merge structural, Notion e auditoria extraído para auxiliares. Referências `> **[Structural/Notion/Auditoria]**` substituem blocos longos.

**Como aplicar:** Copiar do framework source.

```bash
cp {framework_path}/skills/update-framework/SKILL.md .claude/skills/update-framework/SKILL.md
```

**Nota:** Os auxiliares (`STRUCTURAL_MERGE_DETAILS.md`, `NOTION_UPDATE_DETAILS.md`, `AUDIT_DETAILS.md`) são internos do framework e **não precisam ser copiados para o projeto**. A skill update-framework roda a partir do source do framework, onde os auxiliares existem.

### ⚙️ Nenhuma ação necessária

Esta versão é uma refatoração interna. Se você executa `/setup-framework` ou `/update-framework` a partir do clone do framework ou instalação pessoal/plugin, basta atualizar o framework (`git pull` ou reinstalar plugin) — nenhuma mudança no projeto destino é necessária.

## Checklist

- [ ] Framework atualizado (`git pull` no clone ou reinstalar plugin)
- [ ] `/update-framework` funciona normalmente
