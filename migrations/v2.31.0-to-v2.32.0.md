<!-- framework-tag: v2.44.0 framework-file: migrations/v2.31.0-to-v2.32.0.md -->
# Migration v2.31.0 → v2.32.0

> **Como aplicar:** siga cada seção em ordem. Arquivos `structural` precisam de merge manual (ou use `/update-framework` para aplicar automaticamente).

---

## Arquivos novos

### 1. `docs/SKILLS_GUIDE.md` (novo)

**O que é:** catálogo descritivo de todas as 25 skills do framework — o que cada uma faz, quando usar e o que produz. Organizado por fluxo principal e categoria funcional (planejamento, testes/qualidade, segurança, banco/API, frontend/UX, suporte). Inclui seção `## Skills customizadas do projeto` para o projeto adicionar skills próprias.

**Relevante se:** o projeto usa o framework (recomendado para todos — substitui ter que abrir o README de cada skill para entender o que existe).

**Como instalar:**

```bash
mkdir -p docs/
cp {framework_path}/docs/SKILLS_GUIDE.md docs/SKILLS_GUIDE.md
```

**Se decidir não instalar:** sem impacto funcional. O guia é referência descritiva — as skills continuam funcionando normalmente. Perde o ponto de entrada unificado para descobrir qual skill usar.

---

## Skills (structural — nova linha na auditoria de completude)

### `skills/setup-framework/SKILL.md` e `skills/update-framework/SKILL.md` — nova entrada na auditoria

**Motivo:** o setup e o update agora verificam a presença de `docs/SKILLS_GUIDE.md` na auditoria de completude (Categoria 1 / Categoria 2 docs).

**O update-framework já aplica essa verificação automaticamente quando você roda `/update-framework`.** Este content patch só é necessário se as skills `setup-framework` ou `update-framework` estiverem customizadas localmente.

---

## Framework-tags

Após aplicar as mudanças, atualize os framework-tags nos arquivos tocados:

```bash
grep -rl "framework-tag: v2.44.0" --include="*.md" . | xargs sed -i 's/framework-tag: v2\.31\.0/framework-tag: v2.44.0/g'
```

## Verificação

- [ ] `docs/SKILLS_GUIDE.md` instalado
- [ ] Framework-tags atualizados para v2.32.0
