# Migration v2.38.0 → v2.39.0

> Data: 2026-04-15
> Tipo: minor

## Resumo

Nova seção `## Monorepo` no CLAUDE.template.md como fonte de verdade declarativa para projetos monorepo. 3 subsections: Estrutura, Distribuição de framework e Convenções de camada (L0/L2/L3+). Seção opcional — projetos single-repo não a usam.

## Pre-requisitos

- Versao atual do projeto: v2.38.0
- Se estiver em versao anterior, aplique as migrations anteriores primeiro

## Mudancas

### 📝 Structural — secoes novas/removidas

> Estes arquivos tem conteudo customizado pelo projeto. Apenas secoes novas/removidas sao listadas.

#### 1. `CLAUDE.md` (raiz do projeto)

**Secoes adicionadas:**

Adicionar apos a secao `## Contexto de negócio` (ultima secao atual):

```markdown
## Monorepo

{Adaptar se aplicável. Remover esta seção inteira se single-repo.}

### Estrutura

| Sub-projeto | Path | Stack | Responsabilidade |
|---|---|---|---|
| Backend API | `backend/` | Go, PostgreSQL | APIs REST, regras de negócio |
| Frontend Web | `frontend/` | React, TypeScript | Interface web, SPA |
| Shared Libs | `packages/shared/` | TypeScript | Tipos e utilitários compartilhados |

### Distribuição de framework

- **Skills:** {na raiz / por sub-projeto / misto — ex: spec-driven e definition-of-done na raiz, logging e testing por sub-projeto}
- **Agents:** {na raiz / por sub-projeto — ex: security-audit na raiz, component-audit no frontend}
- **Specs/Backlog:** {unificado na raiz / distribuído por sub-projeto / Notion}
- **verify.sh:** {por sub-projeto / orquestrador na raiz + por sub-projeto}

### Convenções de camada

- **L0 (raiz):** {o que vive na raiz — commits, segurança global, mapa de skills universais}
- **L2 (sub-projeto):** {o que é específico — stack, comandos, testes, coverage, skills com exemplos de código}
- **L3+ (sub-domínio):** {se aplicável — ex: `backend/src/payments/` com CLAUDE.md para regras de compliance de pagamentos}

> Níveis abaixo de L2 são opcionais. Usar quando um sub-domínio tem regras suficientemente distintas (compliance, segurança, integração com terceiros) que justifiquem CLAUDE.md próprio. Na dúvida, manter em L2.
```

**Relevante se:** projeto e monorepo. Se single-repo, nao adicionar — a secao inteira e opcional.

**Impacto se nao aplicar:** skills como `/discuss`, `/spec` e `/backlog-update` continuam inferindo a estrutura do monorepo a cada execucao em vez de ler de uma fonte de verdade.

---

### 🔄 Overwrite (substituir arquivo inteiro)

> Estes arquivos nao tem customizacao de projeto — basta copiar do framework.

#### 2. `.claude/skills/update-framework/SKILL.md`

**O que mudou:** Fase 0.5 agora verifica presenca da secao `## Monorepo` no CLAUDE.md e sinaliza ausencia para structural merge. Fase 4 usa a secao como fonte de verdade para paths e stacks (fallback: SETUP_REPORT.md).

**Como aplicar:** Copiar do framework.

**Impacto se nao aplicar:** update-framework nao oferece a secao `## Monorepo` para monorepos existentes.

---

#### 3. `docs/SETUP_GUIDE.md`

**O que mudou:** Exemplos de monorepo agora mostram `## Monorepo` no output gerado. FAQ atualizado mencionando a secao como fonte de verdade.

**Como aplicar:** Copiar do framework.

**Impacto se nao aplicar:** documentacao desatualizada sobre o que o setup gera em monorepos.

---

#### 4. `docs/SPEC_DRIVEN_GUIDE.md`

**O que mudou:** Secao "Specs em monorepos" agora referencia `## Monorepo` como fonte de verdade.

**Como aplicar:** Copiar do framework.

**Impacto se nao aplicar:** guia de specs nao menciona a nova fonte de verdade para monorepos.

---

#### 5. `docs/WORKFLOW_DIAGRAM.md`

**O que mudou:** Bloco de setup monorepo agora inclui preenchimento da secao `## Monorepo`.

**Como aplicar:** Copiar do framework.

**Impacto se nao aplicar:** diagrama de workflow nao reflete o artefato gerado.

---

## Framework-tags

Apos aplicar as mudancas, atualize os framework-tags nos arquivos tocados:

```bash
# Atualizar todos os framework-tags de v2.38.0 para v2.39.0
grep -rl "framework-tag: v2.38.0" --include="*.md" . | xargs sed -i 's/framework-tag: v2.38.0/framework-tag: v2.39.0/g'
```

## Verificacao

Apos aplicar, verifique:

- [ ] Secao `## Monorepo` adicionada ao CLAUDE.md (se monorepo) ou ignorada (se single-repo)
- [ ] Arquivos overwrite substituidos (update-framework, docs)
- [ ] Framework-tags atualizados para v2.39.0
- [ ] `scripts/verify.sh` passa sem erros (se aplicavel)
