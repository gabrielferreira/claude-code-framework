# Migration v2.10.0 → v2.10.1

> Data: 2026-04-03
> Tipo: patch

## Resumo

Adiciona o diagrama de workflow do framework (`docs/WORKFLOW_DIAGRAM.md`) com 7 diagramas ASCII: setup, uso diario, agents, update, ciclo de release, estrategias do MANIFEST e integracao Notion. Demais mudancas sao apenas bump de framework-tag.

## Pre-requisitos

- Versao atual do projeto: v2.10.0
- Se estiver em versao anterior, aplique as migrations anteriores primeiro

## Mudancas

### 🆕 Arquivos novos

#### 1. `docs/WORKFLOW_DIAGRAM.md`

**O que e:** Documento com 7 diagramas ASCII cobrindo os fluxos principais do framework.

**Como aplicar:**

```bash
cp {framework_path}/docs/WORKFLOW_DIAGRAM.md {project_path}/docs/WORKFLOW_DIAGRAM.md
```

**Impacto se nao aplicar:** Documento de referencia visual ausente — funcional, sem impacto operacional.

---

### 🔄 Overwrite (substituir arquivo inteiro)

> Estes arquivos nao tem customizacao de projeto — basta copiar do framework.

#### 1. Todos os 10 agents

**O que mudou:** Framework-tag atualizado para v2.10.1.

**Agents afetados:**
- `.claude/agents/backlog-report.md`
- `.claude/agents/code-review.md`
- `.claude/agents/component-audit.md`
- `.claude/agents/coverage-check.md`
- `.claude/agents/product-review.md`
- `.claude/agents/refactor-agent.md`
- `.claude/agents/security-audit.md`
- `.claude/agents/seo-audit.md`
- `.claude/agents/spec-validator.md`
- `.claude/agents/test-generator.md`

**Como aplicar:**

```bash
cp {framework_path}/agents/*.md {project_path}/.claude/agents/
```

**Impacto se nao aplicar:** Agents com framework-tag desatualizado — funcional, mas inconsistente.

---

#### 2. `specs/TEMPLATE.md` e `specs/DESIGN_TEMPLATE.md`

**O que mudou:** Framework-tag atualizado para v2.10.1.

**Como aplicar:**

```bash
cp {framework_path}/specs/TEMPLATE.md {project_path}/specs/TEMPLATE.md
cp {framework_path}/specs/DESIGN_TEMPLATE.md {project_path}/specs/DESIGN_TEMPLATE.md
```

**Impacto se nao aplicar:** Templates com framework-tag desatualizado — funcional.

---

#### 3. `.claude/plugin.json`

**O que mudou:** Campo `version` atualizado para `2.10.1`.

**Como aplicar:**

```bash
cp {framework_path}/.claude-plugin/plugin.json {project_path}/.claude/plugin.json
```

**Impacto se nao aplicar:** Plugin reportando versao incorreta.

---

### 📝 Structural (adicionar/remover secoes)

#### 1. Todas as skills, docs e `prds/PRD_TEMPLATE.md`

**O que mudou:** Framework-tag atualizado para v2.10.1 em todos os arquivos structural. Nenhuma secao adicionada ou removida.

**Como aplicar:** Atualizar o framework-tag em cada arquivo (ver secao Framework-tags abaixo).

**Impacto se nao aplicar:** Tags desatualizadas — funcional, mas inconsistente.

---

### 👀 Manual (decisao humana)

#### 1. `CLAUDE.md` do projeto (gerado a partir de `CLAUDE.template.md`)

**O que mudou no template:** Apenas framework-tag atualizado para v2.10.1. Nenhuma mudanca de conteudo.

**Sugestao:** Nenhuma acao necessaria alem do bump de tag, se desejar manter consistencia.

---

## Framework-tags

Apos aplicar as mudancas, atualize os framework-tags nos arquivos tocados:

```bash
# Atualizar todos os framework-tags de v2.10.0 para v2.10.1
grep -rl "framework-tag: v2.12.0" --include="*.md" . | xargs sed -i 's/framework-tag: v2.12.0/framework-tag: v2.12.0/g'
```

## Verificacao

Apos aplicar, verifique:

- [ ] `docs/WORKFLOW_DIAGRAM.md` criado
- [ ] Agents (10) substituidos com framework-tag v2.10.1
- [ ] `specs/TEMPLATE.md` e `specs/DESIGN_TEMPLATE.md` atualizados
- [ ] `.claude/plugin.json` com `"version": "2.10.1"`
- [ ] Skills, docs e `prds/PRD_TEMPLATE.md` com framework-tag v2.10.1
- [ ] Framework-tags atualizados para v2.10.1
