<!-- framework-tag: v2.36.0 framework-file: migrations/v2.30.0-to-v2.31.0.md -->
# Migration v2.30.0 → v2.31.0

> **Como aplicar:** siga cada seção em ordem. Arquivos `structural` precisam de merge manual (ou use `/update-framework` para aplicar automaticamente).

---

## Arquivos novos

### 1. `.claude/skills/resume/README.md` (novo)

**O que é:** skill `/resume` para recuperação de sessão após crash, timeout ou context limit. Lê `STATE.md` + execution-plan e apresenta resumo do estado anterior antes de retomar.

**Relevante se:** o projeto usa `STATE.md` para continuidade entre sessões (recomendado para todos).

**Como instalar:**

```bash
mkdir -p .claude/skills/resume/
cp {framework_path}/skills/resume/README.md .claude/skills/resume/README.md
```

**Se decidir não instalar:** sessões que caem no meio de uma task precisam de retomada manual — o `/resume` estrutura esse processo.

---

## Skills (structural — seções adicionadas)

As 16 skills abaixo receberam as seções obrigatórias `## Quando usar`, `## Quando NÃO usar`, `## Checklist` e `## Regras`. O `/update-framework` detecta e oferece adicionar via structural merge.

Se preferir aplicar manualmente, consultar os fontes no framework para o conteúdo exato de cada seção:

| Skill | Arquivo |
|---|---|
| backlog-update | `.claude/skills/backlog-update/SKILL.md` |
| bug-investigation | `.claude/skills/bug-investigation/SKILL.md` |
| dba-review | `.claude/skills/dba-review/README.md` |
| definition-of-done | `.claude/skills/definition-of-done/README.md` |
| docs-sync | `.claude/skills/docs-sync/README.md` |
| golden-tests | `.claude/skills/golden-tests/README.md` |
| logging | `.claude/skills/logging/README.md` |
| mock-mode | `.claude/skills/mock-mode/README.md` |
| prd-creator | `.claude/skills/prd-creator/SKILL.md` |
| security-review | `.claude/skills/security-review/README.md` |
| seo-performance | `.claude/skills/seo-performance/README.md` |
| spec-creator | `.claude/skills/spec-creator/SKILL.md` |
| spec-driven | `.claude/skills/spec-driven/README.md` |
| syntax-check | `.claude/skills/syntax-check/README.md` |
| testing | `.claude/skills/testing/README.md` |
| ux-review | `.claude/skills/ux-review/README.md` |

**Impacto se não aplicar:** skills sem as seções continuam funcionando, mas perdem a estrutura padrão (Quando usar/não usar, Checklist, Regras). O `validate-structure.sh` do framework (script interno) agora falha para skills sem essas seções — projetos não são afetados diretamente.

---

## `PROJECT_CONTEXT.md` (structural — seção nova)

**Seção adicionada:** `## Restrições inegociáveis`

Adicionar antes da seção `## O que este projeto NÃO faz`:

```markdown
## Restrições inegociáveis

> Decisões arquiteturais e padrões fixos que não estão abertos a discussão.
> Toda spec e execution-plan deve respeitar estas restrições.

{Adaptar — exemplos de restrições comuns:}

- **Stack de banco:** PostgreSQL — sem ORM alternativo, sem SQLite em produção
- **Autenticação:** JWT via middleware `auth` — sem sessão em memória, sem cookie sem httpOnly
- **API:** REST com JSON — sem GraphQL, sem gRPC neste projeto
- **Infra:** Kubernetes — sem deploy direto em VMs ou Heroku
- **Cobertura mínima:** 80% em funções de negócio — não negociável por prazo
- {adicionar restrições do projeto}

**Como usar:** ao criar spec ou execution-plan, verificar se a proposta respeita estas restrições. Se conflitar, escalar antes de prosseguir — não contornar silenciosamente.

---
```

**Impacto se não aplicar:** a skill `/spec` (`spec-creator`) não pode executar o Passo 0a (verificação de restrições antes de criar spec). O projeto continua funcionando, mas perde o gate de verificação de restrições arquiteturais.

---

## Content patches (mudanças dentro de seções existentes)

### `skills/spec-creator/SKILL.md` — novo Passo 0a

**Motivo:** integração com a nova seção `## Restrições inegociáveis` do PROJECT_CONTEXT.md — a skill verifica restrições antes de criar a spec.

**Adicionar** entre `### Passo 0 — Detectar modo (repo ou Notion)` e `### Passo 0b — Resolver fonte externa`:

```markdown
### Passo 0a — Verificar restrições inegociáveis

> **Antes de criar:** verificar se `PROJECT_CONTEXT.md` tem seção `## Restrições inegociáveis`. Se sim, a spec não pode propor solução que conflite com essas restrições. Em caso de conflito, escalar ao usuário antes de prosseguir.

---
```

**Impacto se não aplicar:** a skill `/spec` não verifica restrições arquiteturais antes de criar a spec — specs podem ser criadas propondo soluções que conflitem com decisões já tomadas.

---

### `skills/update-framework/SKILL.md` — novos checks na auditoria de completude

**Motivo:** o update-framework precisa detectar a nova skill `/resume` e a nova seção `## Restrições inegociáveis` nos projetos existentes.

**O update-framework já aplica essas verificações automaticamente quando você roda `/update-framework`.** Este content patch só é necessário se a skill `update-framework` estiver customizada localmente e você quiser manter em sincronia.

---

## CLAUDE.md (structural — nova linha na tabela de skills)

**Linha adicionada** na tabela `## Skills — ler ANTES de codificar`:

```markdown
| 26 | Sessão caiu no meio de uma task (crash/timeout/context limit)? | `/resume` — retomada estruturada via STATE.md e execution-plan | ⛔ Sempre |
```

Adicionar como última linha da tabela (antes de `{26+. Skills específicas do domínio do projeto}`).

---

## Framework-tags

Após aplicar as mudanças, atualize os framework-tags nos arquivos tocados:

```bash
grep -rl "framework-tag: v2.36.0" --include="*.md" . | xargs sed -i 's/framework-tag: v2\.30\.0/framework-tag: v2.36.0/g'
```

## Verificação

- [ ] Skill `/resume` instalada em `.claude/skills/resume/README.md`
- [ ] `PROJECT_CONTEXT.md` tem seção `## Restrições inegociáveis`
- [ ] Skills distribuídas têm as 4 seções obrigatórias (ou aplicadas via `/update-framework`)
- [ ] `CLAUDE.md` tem linha `/resume` na tabela de skills
- [ ] Framework-tags atualizados para v2.31.0
