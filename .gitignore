node_modules/
coverage/
# Env vars — padrão que cobre qualquer variante (.env.local, .env.staging, etc.)
# Exceto .env.preview: arquivo de conveniência com valores dummy (sem segredos)
.env
.env.*
!.env.preview
deploy/.deploy.env
*.log
dist/
dist-assistant/
build/
.DS_Store
apply-changes.sh
# Claude Code worktrees
.claude/worktrees/
.claude/projects/
# .claude/plans/ — NÃO ignorar: planos aprovados são persistidos como referência de design
# Playwright
e2e-report/
test-results/
playwright-report/
